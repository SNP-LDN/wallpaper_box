{
  "name": "wallpaper-local-library",
  "version": "1.1.0",
  "description": "壁纸盒：本地 Wallpaper 壁纸文件夹管理器",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "npm run dist:all",
    "dist:installer": "electron-builder --win nsis -c.artifactName=\"wallpaper_box-${version}-setup.${ext}\"",
    "dist:portable": "electron-builder --win portable -c.artifactName=\"wallpaper_box-${version}-portable.${ext}\"",
    "dist:all": "npm run dist:installer && npm run dist:portable"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.wallpaperbox.app",
    "productName": "壁纸盒",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*",
      "assets/**/*",
      "USER_GUIDE.md",
      "CHANGELOG.md",
      "README_EXE.txt",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
